# Plan: Update AGENTS.md

## Goal
Update the existing AGENTS.md with accurate current state so future agents avoid mistakes.

## Changes

### 1. Update file tree (§Current Implementation)
Add two missing modules to the file tree:
- `src/recorder.ts` — Recorder class: session lifecycle, capture spawning, keyboard handling (544 lines)
- `src/entries-store.ts` — JSONL entry persistence for recovery-safe transcript storage

Both are imported by existing modules (cli.ts → recorder, pipeline.ts/finalize.ts → entries-store).

### 2. Everything else stays
Verified all other sections against codebase:
- Build/run commands ✓ (match package.json scripts)
- Key constraints ✓ (match types.ts, storage.ts)
- Transcription quality ✓ (match transcriber.ts flags)
- Output format ✓ (match assembler.ts)
- Finalization pipeline ✓ (match finalize.ts)
- Conventions ✓ (match actual code patterns)
- Critical gotchas ✓ (match Swift and TS code)
- What's not done ✓ (still accurate)
- References ✓

### 3. Out of scope (flag for separate fix)
CLAUDE.md has stale session path (`/tmp/meet-{id}/session.json` → should be `~/.meet/sessions/meet-{id}/session.json`). Not fixing in this change — different file, different concern.

## Verification
- Read back AGENTS.md after writing
- Confirm file tree matches `ls src/*.ts` output
- Confirm no other sections were accidentally modified
